<% if options[:only].include?(:show) %>
	<%= link_to arr, class: 'btn' do %>
		<i class="icon-external-link"></i> Mostrar
	<% end %>
<% end %>
<% if options[:only].include?(:edit) %>
	<%= link_to [:edit].concat(arr), class: 'btn' do %>
		<i class="icon-edit"></i> Editar
	<% end %>
<% end %>
<% if options[:only].include?(:destroy) %>
	<%= link_to arr, class: 'btn btn-danger', method: :delete, data: { confirm: 'Está seguro?' } do %>
		<i class="icon-trash"></i> Destruir
	<% end %>	
<% end %>