= render 'breadcrumbs'
%h1 Cines

%table.table
	%tr
		%th Nombre
		%th Imagen
		%th Información
		%th 
		
	- @cinemas.each do |cinema|
		%tr
			%td= link_to cinema.name, admin_cinema_theaters_path(cinema)
			%td= link_to image_tag(cinema.image, size: "70x100"), cinema.image.to_s
			%td= cinema.information
			%td= render_index_actions [cinema], only: [:edit, :destroy]
%br

= link_to new_admin_cinema_path, class: 'btn' do
	%i.icon-plus
	Nuevo Cine