%h1 Tipos de Función

%table.table
	%tr
		%th Nombre
		%th>

	- @function_types.each do |function_type|
		%tr
			%td= function_type.name
			%td= render_index_actions [function_type], only: [:edit, :destroy]

%br

= link_to [:new, :admin, :function_type], class: 'btn' do
	%i.icon-plus
	Nuevo Function Type
