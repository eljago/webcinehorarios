= render 'breadcrumbs'

%h1 Nueva Función

- if @cinema.name == "Cinemark"
	= render 'parse_cinemark'
- elsif @cinema.name == "Cine Hoyts" || @cinema.name == "Cinemundo"
	= render 'parse_hoyts'
- elsif @cinema.name == "Cineplanet"
	= render 'parse_cineplanet'

= link_to admin_theater_functions_path(@theater, date: @date), class: 'btn' do
	%i.icon-arrow-left
	Volver