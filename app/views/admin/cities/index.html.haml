= render 'breadcrumbs'
%h1 Ciudades

%table.table
	%tr
		%th Nombre
		%th

	-@cities.each do |city|
		%tr
			%td= link_to city.name, admin_city_theaters_path(city)
			%td= render_index_actions [city], only: [:edit, :destroy]

%br

= link_to new_admin_country_city_path(@country), class: 'btn btn-default' do
	%i.icon-plus
	Nueva Ciudad