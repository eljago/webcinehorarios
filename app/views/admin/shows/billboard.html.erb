<% provide(:title, 'Cartelera') %>
<%= render 'breadcrumbs' %>
<h1>Cartelera</h1>

<table class="table">
  <tr>
    <th>Nombre</th>
    <th>Imagen</th>
    <th>Duración</th>
    <th>Estreno</th>
    <th>Rating</th>
    <th></th>
  </tr>

<% @shows.each do |show| %>
  <tr>
    <td><%= show.name %></td>
    <td><%= link_to image_tag(show.image.smallest, size: '50x50'), show.image.to_s if show.image %></td>
    <td><%= show.duration %></td>
    <td><%= show.debut %></td>
    <td><%= show.rating %></td>
		<td><%= render_index_actions [show] %></td>
  </tr>
<% end %>
</table>

<br />
