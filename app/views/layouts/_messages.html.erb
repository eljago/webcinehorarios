<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) %>
		<% type = name %>
		<% type = :danger if name == "error" || name == "recaptcha_error" %>
		<% type = :success if name == "notive" %>
		<%= alert_box msg, context: type, dismissible: true %>
  <% end %>
<% end %>